#include<stdio.h>
int main()
{
    int x,y,sum;
    printf("Input two numbers: ");
    scanf("%d %d",&x,&y);
    sum=x+y;
    printf("First number is %d and Second number is %d Finally The sum is %d",x,y,sum);
    return 0;
}
